[project]
name = "moyu-salary"
version = "2.0.0"
description = "摸鱼助手 — 时薪计算与 AI 摸鱼伴侣"
requires-python = ">=3.10"
dependencies = [
    "streamlit>=1.28",
    "chinese-calendar>=1.9",
    "pandas>=2.0",
    "httpx>=0.25",
]

[project.optional-dependencies]
dev = ["pytest>=7.0"]
